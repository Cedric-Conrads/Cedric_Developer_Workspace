git push -u origin main

-u = --set-upstream

Richtet eine dauerhafte Verbindung zwischen
dem lokalen Branch und dem entfernten Branch ein.

Danach reicht meistens:

git push

anstatt:

git push origin main

Merksatz:

-u = "Merke dir dieses Ziel für zukünftige Pushes."