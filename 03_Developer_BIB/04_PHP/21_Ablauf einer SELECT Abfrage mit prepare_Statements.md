1. Formular absenden (GET)

↓

2. $_GET auslesen

↓

3. isset() prüfen

↓

4. Suchbegriff vorbereiten

↓

5. Wildcards hinzufügen

↓

6. prepare()
→ SQL-Abfrage vorbereiten

↓

7. bind_param()
→ Werte an die Platzhalter binden

↓

8. execute()
→ SQL-Abfrage ausführen

↓

9. get_result()
→ Ergebnis der Datenbank erhalten

↓

10. while()

↓

11. fetch_assoc()
→ Zeile für Zeile aus dem Ergebnis holen




Merksätze
prepare() → SQL vorbereiten
bind_param() → Werte an ? binden
execute() → SQL ausführen
get_result() → Ergebnis zurückgeben
fetch_assoc() → Eine Zeile als assoziatives Array holen
while → Wiederholt den Vorgang, bis keine Zeilen mehr vorhanden sind