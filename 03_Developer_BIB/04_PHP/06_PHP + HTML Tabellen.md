PHP + HTML Tabellen

<table>
=
Wird genau einmal erstellt.

<th>
=
Die Tabellenüberschrift.
Wird genau einmal erstellt.

while ($row = $result->fetch_assoc())

=
Läuft für jeden Datensatz einmal.

Bei jedem Durchlauf wird eine neue:

<tr>

mit mehreren

<td>

erzeugt.

Merksatz:

Eine Tabelle.
Viele Zeilen.
Eine Zeile pro Datensatz.