# Cedrics Developer BIB v2

Ergänzte Version deiner bestehenden BIB. Die alte Struktur und deine Notizen wurden beibehalten.

## Backend-Ablauf

```text
Formular → GET/POST → isset() → prepare() → bind_param() → execute() → get_result() → fetch_assoc() → Ausgabe
```
